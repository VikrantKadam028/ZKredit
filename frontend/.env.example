VITE_API_URL=http://localhost:8000

# Google OAuth Client ID (same one used by the backend's GOOGLE_CLIENT_ID) —
# needed for the "Sign in with Google" button to render. Leave blank to
# disable it (email/password still works).
VITE_GOOGLE_CLIENT_ID=