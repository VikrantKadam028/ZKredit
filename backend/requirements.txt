fastapi
uvicorn[standard]
sqlalchemy
pydantic
onnxruntime
numpy
python-multipart

# Auth: JWT + password hashing + Google OAuth token verification
python-jose[cryptography]
bcrypt
google-auth
email-validator

# Postgres driver (used when DATABASE_URL points at postgres, e.g. in Docker)
psycopg2-binary

# For real ZK proof generation (POST /applications/{id}/generate-proof).
# This does make the backend heavier than a pure-inference service — that's
# an intentional tradeoff for this project's scope. See README for splitting
# this into a separate "prover" service later if needed.
ezkl
requests
